<section class="hero">
  <div class="carousel h-screen">
    {% for slide in slides.photos %}
      <div class="slide w-full h-screen">
        <img src="{{slide.image}}" alt="{{slide.title}}" class="w-full h-full object-cover">
      </div>
    {% endfor %}
  </div>
</section>
